"""Shared cellier-layer logic for Qt single-field appearance controls.

Placeholder for Phase 2.  The ``AppearanceFieldMixin`` (design doc section 6.2)
will factor the echo filter, field filter, event construction,
``subscription_specs``, and ``close`` shared by the Qt single-field appearance
widgets.  The anywidget side handles these fields inline in its composite
``ControlPanel`` instead, so nothing lives here yet.
"""
